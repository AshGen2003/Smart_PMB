# No sysops models are currently registered with Django's built-in admin
# site — they're managed instead through the app's own DRF admin API
# (see sysops/views.py) consumed by the Next.js admin frontend.
from django.contrib import admin

# Register your models here.
